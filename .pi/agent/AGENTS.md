# Global instructions

## GitHub
When interacting with GitHub (PRs, issues, releases, reviews, CI status, etc.),
prefer the `gh` CLI over raw `git` remote operations or direct API calls.
Assume `gh` is installed and authenticated.
